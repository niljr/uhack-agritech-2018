import React, { PureComponent } from 'react';

export default class Farmer extends PureComponent {
    render() {
        return (
            <div>
                Farmer
            </div>
        );
    }
}