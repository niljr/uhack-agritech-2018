import React, { PureComponent } from 'react';

export default class AddFarmers extends PureComponent {
    render() {
        return (
            <div>
                Add Farmers
            </div>
        );
    }
}
